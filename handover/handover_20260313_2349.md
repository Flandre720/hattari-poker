# 引き継ぎドキュメント — 2026-03-13 23:49

## 概要
v1.0.1 → v1.0.2 の変更。7項目の機能修正、ゲーム名リネーム、タイトルロゴ差し替え。

## コミット一覧（`81b00d7`→`86fb015`）

| コミット | 内容 |
|---------|------|
| `c331249` | 7項目の修正（場カードソート/プレイログ/歯車位置/退室ボタン/視認性/チャレンジ演出/称号ユニーク化） |
| `f8def7c` | ゴキブリポーカー → はったりポーカー（著作権対策） |
| `6de0f79` | AI生成タイトルロゴに差し替え |
| `72b01ac` | ユーザー提供のタイトルロゴに差し替え |
| `e1bf7df` | ロゴ拡大(3倍)+背景なじみ調整 |
| `86fb015` | v1.0.2: 最終調整+コミット |

## 変更詳細（18ファイル, +453/-95行）

### 1. 場カードの並び順ソート
- `OnlineGameBoard.tsx`, `OnlineGameOverScreen.tsx` で `CREATURE_TYPES` 定義順にソート

### 2. リアルタイムプレイログ
- `server.ts`: `replayLog` をゲーム中も常時クライアントに送信
- `OnlineGameBoard.tsx`: 画面左側に `play-log-panel` を追加（ターン別タイムライン表示、自動スクロール）
- `index.css`: `.play-log-panel` 関連スタイル追加（モバイルでは非表示）

### 3. 歯車ボタン位置修正
- `index.css`: `.settings-gear-btn` を `top: 12px` → `bottom: 72px` に移動

### 4. 退室ボタン（ホスト退室→強制終了）
- `SettingsModal.tsx`: 退室ボタン+確認ダイアログ追加
- `App.tsx`: `SettingsButton` に `onLeaveRoom` prop渡し
- `server.ts`: `leave_room` ハンドラにホスト退室時の `room_closed` イベント発行+ルーム強制削除
- `useSocket.ts`: `room_closed` イベントハンドラ追加（全state初期化）
- `types.ts`: `ServerToClientEvents` に `room_closed` 追加

### 5. テキスト視認性改善
- `index.css`: `--text-secondary: #8888a0` → `#c0c0d0`

### 6. チャレンジ宣言演出
- `OnlineGameBoard.tsx`: REVEAL_RESULT表示前に「これは○○だ！」「これは○○じゃない！」を1.5秒表示
- `index.css`: `.challenge-declaration-overlay` アニメーションCSS追加

### 7. 称号ユニーク化
- `server.ts`: `calculateTitles()` を全面改修。`awardUnique()` ヘルパーで各称号をスコア最大の1人のみに付与。同率は付与しない。

### 8. ゲーム名リネーム
- 全ソースファイルのコメント・UI表示テキストを「ゴキブリポーカー」→「はったりポーカー」に一括置換
- `index.html`: `<title>` → 「はったりポーカー」、`lang="ja"`

### 9. タイトルロゴ
- ユーザー提供の `title_logo.png` に差し替え
- CSS: サイズ3倍(`1200px`)、`mix-blend-mode: screen` + `brightness(0.85)` で背景なじみ

## ビルド成果物
- `release/cockroach-poker.exe` — v1.0.2
- `release/dist/` — フロントエンド
- `release/cloudflared.exe` — 62.8MB（既存スキップ）
